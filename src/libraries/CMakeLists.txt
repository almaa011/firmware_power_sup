
# Set the path to FreeRTOS sources
set(FREERTOS_DIR ${CMAKE_CURRENT_LIST_DIR}/FreeRTOS CACHE PATH "FreeRTOS Source Location")

# Set the path to CMSIS sources
set(CMSIS_DIR ${CMAKE_CURRENT_LIST_DIR}/CMSIS CACHE PATH "CMSIS Source Location")

# Set the path to CMSIS sources
set(CMSIS_DSP_DIR ${CMAKE_CURRENT_LIST_DIR}/CMSIS_DSP CACHE PATH "CMSIS DSP Source Location")

# Set the path to the UMNSVP shared libraries.
set(UMNSVP_DIR ${CMAKE_CURRENT_LIST_DIR}/UMNSVP CACHE PATH "UMNSVP Shared Libraries Source Location")

# Set the path to the UMNSVP shared libraries.
set(SEGGER_DIR ${CMAKE_CURRENT_LIST_DIR}/SEGGER CACHE PATH "SEGGER Shared Libraries Source Location")

# Set the path to fatfs
set(FATFS_DIR ${CMAKE_CURRENT_LIST_DIR}/fatfs CACHE PATH "FatFs Source Location")

